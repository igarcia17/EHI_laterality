})
# Combinar todas las muestras en una matriz
expr_matrix <- do.call(cbind, expr_list)
View(expr_matrix)
files
colnames(expr_matrix) <- input$ID
rm(expr_list)
gc()
expr_matrix <- as.data.frame(expr_matrix)
View(expr_matrix)
rownames(expr_matrix)
rownames(expr_matrix)[63145]
rownames(expr_matrix)[63144]
rownames(expr_matrix)[63143]
rownames(expr_matrix)[63142]
rownames(expr_matrix)[63141]
rownames(expr_matrix)[63140]
expr_matrix <- as.data.frame(expr_matrix)[-63141:63145,]
expr_matrix <- as.data.frame(expr_matrix)[-c(63141:63145),]
rownames(expr_matrix)[63140]
#Originally 63144 genes
keep <- rowSums(expr_matrix > 50) > (ncol(expr_matrix) * 0.9)
df <- expr_matrix[keep,] #drops to 15511
#Originally 63144 genes
keep <- rowSums(expr_matrix > 40) > (ncol(expr_matrix) * 0.9)
df <- expr_matrix[keep,] #drops to 15511
#Originally 63144 genes
keep <- rowSums(expr_matrix > 50) > (ncol(expr_matrix) * 0.9)
df <- expr_matrix[keep,] #drops to 15511
#Originally 63144 genes
keep <- rowSums(expr_matrix > 25) > (ncol(expr_matrix) * 0.9)
df <- expr_matrix[keep,] #drops to 10,748
rm(keep,expr_matrix) #Remove because big size
#Order metadata
all(colnames(df) %in% input$ID)
meta <- input[match(colnames(df), input$ID), ]
rm(list = ls())
########################Parameters
cutoffs <- c("0", "60", "90")
oldies <- T
filesF <- "RNAseq-counts_blood/"
input <- as.data.frame(readxl::read_xlsx("All_data_available_genetic_exp.xlsx"
, sheet = 1))%>%
mutate(across(
where(~ n_distinct(., na.rm = TRUE) == 2),
as.factor
)) %>% mutate(Diagnostic = factor(Diagnostic),
Cutoff0 = factor(Cutoff0),
Cutoff40= factor(Cutoff40),
Cutoff60 = factor(Cutoff60),
Cutoff80 = factor(Cutoff80),Cutoff90 = factor(Cutoff90),
Age = as.numeric(Age),
Score10items =as.numeric(Score10items),
PRS_handedness= as.numeric(PRS_handedness),
PRS_amb = as.numeric(PRS_amb),
PRS_BD = as.numeric(PRS_BD),
PRS_SCZ = as.numeric(PRS_SCZ),
Age_group =factor(Age_group),
WAVE = factor(WAVE)
)%>%
mutate(across(
c(Cutoff0, Cutoff40, Cutoff60, Cutoff80, Cutoff90),
~ factor(.x, levels = c("RIGHT", "MIXED", "LEFT"))))%>%
mutate(across(
paste0("Cutoff", cutoffs),
~ factor(ifelse(.x == "MIXED",
"NON-LATERAL",
"LATERAL"),
levels = c("LATERAL", "NON-LATERAL")),
.names = "Lateral_Cutoff{str_remove(.col, 'Cutoff')}"
))%>%
mutate(across(
paste0("Cutoff", cutoffs),
~ factor(ifelse(.x == "RIGHT",
"RH",
"NRH"),
levels = c("RH", "NRH")),
.names = "NRH_{str_remove(.col, 'Cutoff')}"
))
sapply(input, class)
if(oldies){
input <- input %>%
filter(Oldies_dataset == "YES") %>%
filter(has_RNA=="YES")%>%droplevels()
}else{
input <- input%>%filter(has_RNA=="YES")%>%droplevels()
}
#Load genetic expression data
files <- input$File
files <- paste0(filesF, input$File)
expr_list <- lapply(files, function(f) {
df <- read.table(f,
header = F,
sep = "\t",
stringsAsFactors = FALSE,
check.names = FALSE)
counts <- df[, 2]
names(counts) <- df[, 1]
return(counts)
})
library(edgeR)
library(limma)
install.packages("edgeR")
if (!requireNamespace("BiocManager", quietly = TRUE))
install.packages("BiocManager")
BiocManager::install("edgeR")
library(dplyr)
library(tidyverse)
library(ggplot2)
library(tidyr)
library(openxlsx)
library(edgeR)
library(limma)
workingD <- rstudioapi::getActiveDocumentContext()$path
setwd(dirname(workingD))
rm(list = ls())
########################Parameters
cutoffs <- c("0", "60", "90")
oldies <- T
filesF <- "RNAseq-counts_blood/"
input <- as.data.frame(readxl::read_xlsx("All_data_available_genetic_exp.xlsx"
, sheet = 1))%>%
mutate(across(
where(~ n_distinct(., na.rm = TRUE) == 2),
as.factor
)) %>% mutate(Diagnostic = factor(Diagnostic),
Cutoff0 = factor(Cutoff0),
Cutoff40= factor(Cutoff40),
Cutoff60 = factor(Cutoff60),
Cutoff80 = factor(Cutoff80),Cutoff90 = factor(Cutoff90),
Age = as.numeric(Age),
Score10items =as.numeric(Score10items),
PRS_handedness= as.numeric(PRS_handedness),
PRS_amb = as.numeric(PRS_amb),
PRS_BD = as.numeric(PRS_BD),
PRS_SCZ = as.numeric(PRS_SCZ),
Age_group =factor(Age_group),
WAVE = factor(WAVE)
)%>%
mutate(across(
c(Cutoff0, Cutoff40, Cutoff60, Cutoff80, Cutoff90),
~ factor(.x, levels = c("RIGHT", "MIXED", "LEFT"))))%>%
mutate(across(
paste0("Cutoff", cutoffs),
~ factor(ifelse(.x == "MIXED",
"NON-LATERAL",
"LATERAL"),
levels = c("LATERAL", "NON-LATERAL")),
.names = "Lateral_Cutoff{str_remove(.col, 'Cutoff')}"
))%>%
mutate(across(
paste0("Cutoff", cutoffs),
~ factor(ifelse(.x == "RIGHT",
"RH",
"NRH"),
levels = c("RH", "NRH")),
.names = "NRH_{str_remove(.col, 'Cutoff')}"
))
sapply(input, class)
if(oldies){
input <- input %>%
filter(Oldies_dataset == "YES") %>%
filter(has_RNA=="YES")%>%droplevels()
}else{
input <- input%>%filter(has_RNA=="YES")%>%droplevels()
}
#Load genetic expression data
files <- input$File
files <- paste0(filesF, input$File)
expr_list <- lapply(files, function(f) {
df <- read.table(f,
header = F,
sep = "\t",
stringsAsFactors = FALSE,
check.names = FALSE)
counts <- df[, 2]
names(counts) <- df[, 1]
return(counts)
})
expr_matrix <- do.call(cbind, expr_list)
colnames(expr_matrix) <- input$ID
rm(expr_list)
gc()
expr_matrix <- expr_matrix[-c(63141:63145),]
all(colnames(df) %in% input$ID)
meta <- input[match(colnames(df), input$ID), ]
class(expr_matrix)
mode(expr_matrix)
dge <- DGEList(counts = expr_matrix)
dge <- calcNormFactors(dge, method = "TMM")
#Make a dge object of EdgeR
dge <- DGEList(counts = expr_matrix)
# Filtrar genes poco expresados
keep <- filterByExpr(dge, group = input$Handedness)
# Filtrar genes poco expresados
keep <- filterByExpr(dge, group = input$NRH_0)
dge <- dge[keep, , keep.lib.sizes = FALSE]
# Normalización TMM
dge <- calcNormFactors(dge, method = "TMM")
?filterByExpr
sum(keep)
args(filterByExpr)
keep <- filterByExpr(dge, group = input$NRH_0, min.count=20, min.total.count=25,
min-prop=0.5) #Minimum 20 counts in 50% of NRH
keep <- filterByExpr(dge, group = input$NRH_0, min.count=20, min.total.count=25,
min.prop=0.5) #Minimum 20 counts in 50% of NRH
sum(keep)
dge <- dge[keep, , keep.lib.sizes = FALSE]
# Normalize to transcripts per million
dge <- calcNormFactors(dge, method = "TMM")
colnames(meta)
meta <- meta%>%
select(-c("has_RNA", 'PRS_handedness', 'PRS_amb', 'PRS_BD', 'PRS_SCZ',
'Oldies_dataset', 'Cutoff0', 'Cutoff40', 'Cutoff60', 'Cutoff80',
'Cutoff90', 'PC1_all', 'PC1_oldies', 'PC2_all', 'PC2_oldies',
'PC3_all', 'PC3_oldies','PC4_all', 'PC4_oldies'))
#Model design
design <- model.matrix(~ NRH_0 + SEX + ns(Age, df=3) # permite modelizacion polinomica de la edad
+ RIN + Ethnicity + WAVE, data = meta)
library(splines)
#Model design
design <- model.matrix(~ NRH_0 + SEX + ns(Age, df=3) # permite modelizacion polinomica de la edad
+ RIN + Ethnicity + WAVE, data = meta)
sum(is.na(meta$NRH_0))
sum(is.na(meta$SEX))
sum(is.na(meta$Age))
sum(is.na(meta$RIN))
sum(is.na(meta$Ethnicity))
sum(is.na(meta$WAVE))
summary(meta$Age)
sum(is.nan(meta$Age))
sum(is.infinite(meta$Age))
sum(!is.finite(meta$Age))
summary(meta$Age)
View(meta)
#Ensrue metadata is correctly ordered
all(colnames(df) %in% input$ID)
meta <- input[match(colnames(df), input$ID), ]
dim(meta)
dim(input)
#Ensrue metadata is correctly ordered
all(colnames(expr_matrix) %in% input$ID)
meta <- input[match(colnames(expr_matrix), input$ID), ]
meta <- meta%>%
select(-c("has_RNA", 'PRS_handedness', 'PRS_amb', 'PRS_BD', 'PRS_SCZ',
'Oldies_dataset', 'Cutoff0', 'Cutoff40', 'Cutoff60', 'Cutoff80',
'Cutoff90', 'PC1_all', 'PC1_oldies', 'PC2_all', 'PC2_oldies',
'PC3_all', 'PC3_oldies','PC4_all', 'PC4_oldies'))
#Model design
design <- model.matrix(~ NRH_0 + SEX + ns(Age, df=3) # permite modelizacion polinomica de la edad
+ RIN + Ethnicity + WAVE, data = meta)
v <- voom(dge, design, plot = TRUE)
#Fit model
set.seed(13)
fit <- lmFit(v, design)
fit <- eBayes(fit)
topTable(fit)
results <- topTable(fit, coef = "NRH_0", number = Inf)
colnames(fit$coefficients)
results <- topTable(fit, coef = "NRH_0NRH", number = Inf)
View(results)
symbol <- mapIds(get('org.Hs.eg.db'), keys=row.names(results), column="SYMBOL",
keytype="ENSEMBL", multiVals="first") #to obtain gene symbols
library('org.Hs.eg.db')
if (!requireNamespace("BiocManager", quietly = TRUE))
install.packages("BiocManager")
BiocManager::install("org.Hs.eg.db")
library('org.Hs.eg.db')
results <- topTable(fit, coef = "NRH_0NRH", number = Inf)
symbol <- mapIds(get('org.Hs.eg.db'), keys=row.names(results), column="SYMBOL",
keytype="ENSEMBL", multiVals="first") #to obtain gene symbols
description <- mapIds(get('org.Hs.eg.db'), keys=row.names(results),
column="GENENAME", keytype="ENSEMBL",
multiVals="first") #to obtain description
results <- cbind(symbol, results)
results$description <- description
View(results)
writexl::write_xlsx(results, file="DEGs_blood_RNA_NRH_0.xlsx", quote=FALSE, sep="\t", col.names=NA)
writexl::write_xlsx(results, path="DEGs_blood_RNA_NRH_0.xlsx", quote=FALSE, sep="\t", col.names=NA)
writexl::write_xlsx(results, path="DEGs_blood_RNA_NRH_0.xlsx"
)
writexl::write_xlsx(results, path="DEGs_blood_RNA_NRH_0.xlsx",col_names = T)
writexl::write_xlsx(results, path="DEGs_blood_RNA_NRH_0.xlsx",col_names = T)
View(results)
?writexl::write_xlsx
results$Gene <- rownames(results)
results <- results[, c("Gene", setdiff(colnames(results), "Gene"))]
writexl::write_xlsx(results, path="DEGs_blood_RNA_NRH_0.xlsx",col_names = T)
library(EnhancedVolcano, quietly = TRUE)
if (!requireNamespace("BiocManager", quietly = TRUE))
install.packages("BiocManager")
BiocManager::install("EnhancedVolcano")
library(EnhancedVolcano, quietly = TRUE)
library(clusterProfiler, quietly = TRUE)
if (!requireNamespace("BiocManager", quietly = TRUE))
install.packages("BiocManager")
BiocManager::install("clusterProfiler")
library(EnhancedVolcano, quietly = TRUE)
library(clusterProfiler, quietly = TRUE)
library(msigdbr, quietly = T)
EnhancedVolcano(results, lab = results$symbol, x = 'logFC', y = 'P.Value',
pCutoff = 0.000002, FCcutoff= 0.3,
#pCutOff is p value for last significant acc to adjp value
ylim = c(0, 11), xlim = c(-0.6, 0.6), labSize = 3,
legendLabSize = 9, legendIconSize = 5, drawConnectors = TRUE,
widthConnectors = 0.5, max.overlaps = 80, title = '', arrowheads = FALSE,
subtitle= '', gridlines.major = FALSE, gridlines.minor = FALSE)
EnhancedVolcano(results, lab = results$symbol, x = 'logFC', y = 'P.Value',
pCutoff = 0.02, FCcutoff= 0.3,
#pCutOff is p value for last significant acc to adjp value
ylim = c(0, 11), xlim = c(-0.6, 0.6), labSize = 3,
legendLabSize = 9, legendIconSize = 5, drawConnectors = TRUE,
widthConnectors = 0.5, max.overlaps = 80, title = '', arrowheads = FALSE,
subtitle= '', gridlines.major = FALSE, gridlines.minor = FALSE)
EnhancedVolcano(results, lab = results$symbol, x = 'logFC', y = 'P.Value',
pCutoff = 0.05, FCcutoff= 0.3,
#pCutOff is p value for last significant acc to adjp value
ylim = c(0, 11), xlim = c(-0.6, 0.6), labSize = 3,
legendLabSize = 9, legendIconSize = 5, drawConnectors = TRUE,
widthConnectors = 0.5, max.overlaps = 80, title = '', arrowheads = FALSE,
subtitle= '', gridlines.major = FALSE, gridlines.minor = FALSE)
invisible(dev.off())
invisible(dev.off())
jpeg(filename = paste0("Volcano_NRH.jpeg"), units="in", width=8, height=12, res=300)
EnhancedVolcano(results, lab = results$symbol, x = 'logFC', y = 'P.Value',
pCutoff = 0.05, FCcutoff= 0.3,
#pCutOff is p value for last significant acc to adjp value
ylim = c(0, 11), xlim = c(-0.6, 0.6), labSize = 3,
legendLabSize = 9, legendIconSize = 5, drawConnectors = TRUE,
widthConnectors = 0.5, max.overlaps = 80, title = '', arrowheads = FALSE,
subtitle= '', gridlines.major = FALSE, gridlines.minor = FALSE)
invisible(dev.off())
#Perform GSEA
db_sets <- msigdbr(species = 'Homo sapiens', collection = "C5",
subcategory = NULL)%>%
dplyr::select(gs_name, ensembl_gene)
results <- results[complete.cases(results), ]
geneList <- results$t
names(geneList) <- rownames(results)
geneList <- sort(geneList, decreasing = TRUE)
set.seed(13)
egs <- GSEA(geneList = geneList, pvalueCutoff = 0.05, eps = 0, pAdjustMethod = "BH",
seed = T, TERM2GENE = db_sets) #for more accurate p value set eps to 0
egs_df <- data.frame(egs@result)
egs_df <- egs_df[, -c(1,2)]
egs_df$col <- rownames(egs_df)
egs_df <- egs_df[, c("col", setdiff(colnames(egs_df), "col"))]
writexl::write_xlsx(egs_df, path="DEGs_blood_RNA_NRH_0_GSEA.xlsx",col_names = T)
View(egs_df)
#####Now only for BD
input <- as.data.frame(readxl::read_xlsx("All_data_available_genetic_exp.xlsx"
, sheet = 1))%>%
mutate(across(
where(~ n_distinct(., na.rm = TRUE) == 2),
as.factor
)) %>% mutate(Diagnostic = factor(Diagnostic),
Cutoff0 = factor(Cutoff0),
Cutoff40= factor(Cutoff40),
Cutoff60 = factor(Cutoff60),
Cutoff80 = factor(Cutoff80),Cutoff90 = factor(Cutoff90),
Age = as.numeric(Age),
Score10items =as.numeric(Score10items),
PRS_handedness= as.numeric(PRS_handedness),
PRS_amb = as.numeric(PRS_amb),
PRS_BD = as.numeric(PRS_BD),
PRS_SCZ = as.numeric(PRS_SCZ),
Age_group =factor(Age_group),
WAVE = factor(WAVE)
)%>%
mutate(across(
c(Cutoff0, Cutoff40, Cutoff60, Cutoff80, Cutoff90),
~ factor(.x, levels = c("RIGHT", "MIXED", "LEFT"))))%>%
mutate(across(
paste0("Cutoff", cutoffs),
~ factor(ifelse(.x == "MIXED",
"NON-LATERAL",
"LATERAL"),
levels = c("LATERAL", "NON-LATERAL")),
.names = "Lateral_Cutoff{str_remove(.col, 'Cutoff')}"
))%>%
mutate(across(
paste0("Cutoff", cutoffs),
~ factor(ifelse(.x == "RIGHT",
"RH",
"NRH"),
levels = c("RH", "NRH")),
.names = "NRH_{str_remove(.col, 'Cutoff')}"
))
sapply(input, class)
if(oldies){
input <- input %>%
filter(Oldies_dataset == "YES") %>%
filter(has_RNA=="YES")%>%filter(STATUS=="YES")%>%droplevels()
}else{
input <- input%>%filter(has_RNA=="YES")%>%filter(STATUS=="YES")%>%droplevels()
}
View(input)
#Load genetic expression data
files <- input$File
files <- paste0(filesF, input$File)
expr_list <- lapply(files, function(f) {
df <- read.table(f,
header = F,
sep = "\t",
stringsAsFactors = FALSE,
check.names = FALSE)
counts <- df[, 2]
names(counts) <- df[, 1]
return(counts)
})
expr_matrix <- do.call(cbind, expr_list)
colnames(expr_matrix) <- input$ID
rm(expr_list)
gc()
expr_matrix <- expr_matrix[-c(63141:63145),]
#Ensrue metadata is correctly ordered
all(colnames(expr_matrix) %in% input$ID)
meta <- input[match(colnames(expr_matrix), input$ID), ]
meta <- meta%>%
select(-c("has_RNA", 'PRS_handedness', 'PRS_amb', 'PRS_BD', 'PRS_SCZ',
'Oldies_dataset', 'Cutoff0', 'Cutoff40', 'Cutoff60', 'Cutoff80',
'Cutoff90', 'PC1_all', 'PC1_oldies', 'PC2_all', 'PC2_oldies',
'PC3_all', 'PC3_oldies','PC4_all', 'PC4_oldies'))
#Make a dge object of EdgeR + filter
dge <- DGEList(counts = expr_matrix)
keep <- filterByExpr(dge, group = input$NRH_0, min.count=20, min.total.count=25,
min.prop=0.5) #Minimum 20 counts in 50% of NRH
sum(keep)
dge <- dge[keep, , keep.lib.sizes = FALSE]
# Normalize
dge <- calcNormFactors(dge, method = "TMM")
#Model design
design <- model.matrix(~ NRH_0 + SEX + ns(Age, df=3) # permite modelizacion polinomica de la edad
+ RIN + Ethnicity + WAVE, data = meta)
v <- voom(dge, design, plot = TRUE)
#Fit model
set.seed(13)
fit <- lmFit(v, design)
fit <- eBayes(fit)
#To check what comparison to adress
colnames(fit$coefficients)
results <- topTable(fit, coef = "NRH_0NRH", number = Inf)
symbol <- mapIds(get('org.Hs.eg.db'), keys=row.names(results), column="SYMBOL",
keytype="ENSEMBL", multiVals="first") #to obtain gene symbols
description <- mapIds(get('org.Hs.eg.db'), keys=row.names(results),
column="GENENAME", keytype="ENSEMBL",
multiVals="first") #to obtain description
results <- cbind(symbol, results)
results$description <- description
results$Gene <- rownames(results)
results <- results[, c("Gene", setdiff(colnames(results), "Gene"))]
writexl::write_xlsx(results, path="DEGs_blood_RNA_NRH_0_onlyBD.xlsx",col_names = T)
View(results)
jpeg(filename = paste0("Volcano_NRH_onlyBD.jpeg"), units="in", width=8, height=12, res=300)
EnhancedVolcano(results, lab = results$symbol, x = 'logFC', y = 'P.Value',
pCutoff = 0.05, FCcutoff= 0.3,
#pCutOff is p value for last significant acc to adjp value
ylim = c(0, 11), xlim = c(-0.6, 0.6), labSize = 3,
legendLabSize = 9, legendIconSize = 5, drawConnectors = TRUE,
widthConnectors = 0.5, max.overlaps = 80, title = '', arrowheads = FALSE,
subtitle= '', gridlines.major = FALSE, gridlines.minor = FALSE)
invisible(dev.off())
#Perform GSEA
db_sets <- msigdbr(species = 'Homo sapiens', collection = "C5",
subcategory = NULL)%>%
dplyr::select(gs_name, ensembl_gene)
results <- results[complete.cases(results), ]
geneList <- results$t
names(geneList) <- rownames(results)
geneList <- sort(geneList, decreasing = TRUE)
set.seed(13)
egs <- GSEA(geneList = geneList, pvalueCutoff = 0.05, eps = 0, pAdjustMethod = "BH",
seed = T, TERM2GENE = db_sets) #for more accurate p value set eps to 0
egs_df <- data.frame(egs@result)
egs_df <- egs_df[, -c(1,2)]
egs_df$col <- rownames(egs_df)
egs_df <- egs_df[, c("col", setdiff(colnames(egs_df), "col"))]
writexl::write_xlsx(egs_df, path="DEGs_blood_RNA_NRH_0_GSEA_onlyBD.xlsx",col_names = T)
View(results)
View(egs_df)
design <- model.matrix(~ NRH_0 + SEX + ns(Age, df=3) # permite modelizacion polinomica de la edad
+ RIN + Ethnicity, data = meta)
v <- voom(dge, design, plot = TRUE)
#Fit model
set.seed(13)
fit <- lmFit(v, design)
fit <- eBayes(fit)
#To check what comparison to adress
colnames(fit$coefficients)
results <- topTable(fit, coef = "NRH_0NRH", number = Inf)
symbol <- mapIds(get('org.Hs.eg.db'), keys=row.names(results), column="SYMBOL",
keytype="ENSEMBL", multiVals="first") #to obtain gene symbols
description <- mapIds(get('org.Hs.eg.db'), keys=row.names(results),
column="GENENAME", keytype="ENSEMBL",
multiVals="first") #to obtain description
results <- cbind(symbol, results)
results$description <- description
results$Gene <- rownames(results)
results <- results[, c("Gene", setdiff(colnames(results), "Gene"))]
View(results)
results <- results[complete.cases(results), ]
geneList <- results$t
names(geneList) <- rownames(results)
geneList <- sort(geneList, decreasing = TRUE)
set.seed(13)
egs <- GSEA(geneList = geneList, pvalueCutoff = 0.05, eps = 0, pAdjustMethod = "BH",
seed = T, TERM2GENE = db_sets) #for more accurate p value set eps to 0
egs_df <- data.frame(egs@result)
egs_df <- egs_df[, -c(1,2)]
egs_df$col <- rownames(egs_df)
egs_df <- egs_df[, c("col", setdiff(colnames(egs_df), "col"))]
View(egs_df)
